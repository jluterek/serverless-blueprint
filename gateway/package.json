{
  "name": "blueprint-gateway",
  "version": "1.0.0",
  "description": "A sample serverless shared gateway.",
  "main": "",
  "scripts": {
    "lint": "echo \"Error: no linter specified\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "serverless deploy",
    "remove": "serverless remove"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {}
}
